import React from 'react';

import StockPage from './containers/StockPage';

const App: React.FC = (props) => {
    return (
        <div>
            <StockPage />
        </div>
    );
};

export default App;
